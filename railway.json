{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "healthcheckPath": "/health",
    "startCommand": "uvicorn mcp_server_servicenow.server:app --host=0.0.0.0 --port=$PORT --log-level debug"
  }
}

